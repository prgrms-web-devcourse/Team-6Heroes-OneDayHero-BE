package com.sixheroes.onedayherodomain.missionbookmark.repository;

import com.sixheroes.onedayherodomain.missionbookmark.MissionBookmark;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MissionBookmarkRepository extends JpaRepository<MissionBookmark, Long> {
}
