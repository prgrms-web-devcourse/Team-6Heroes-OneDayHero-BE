dependencies {
    api 'org.springframework.boot:spring-boot-starter-data-jpa'
    api 'com.h2database:h2'
}

tasks.named('test') {
    useJUnitPlatform()
}

jar {
    enabled = true
}

bootJar {
    enabled = false
}
