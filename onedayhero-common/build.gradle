dependencies {

ext {
    mysqlJdbcDriverVersion = '8.0.33'
}

dependencies {
    //MySQL
    api "mysql:mysql-connector-java:${mysqlJdbcDriverVersion}"
}

tasks.named('test') {
    useJUnitPlatform()
}

jar {
    enabled = true
}

bootJar {
    enabled = false
}
